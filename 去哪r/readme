执行qunar.py

需要node+jsdom配置好

csrtoken只是个例子，还有很多cookies未上传。另外获取params的_m_参数、Alina(cookie)使用代码执行需要扣js代码，但目前这两参数并非很重要，非必须。我没精力也没兴趣去扣了，浏览器上执行即可。。。

最后通告：因为去哪儿的特殊设定，总之，，若使用从网站上拿来的cookie、pre参数，也许你的程序只能爬一次，不能再多了。
因此，如果出现这个问题。首先pre应完全自己获取，甚至cookies也一样(缓存cookies例外)。然后你就能多次运行。。